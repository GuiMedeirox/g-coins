{
  "name": "@g-coins/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(lint placeholder — shared)\"",
    "test": "echo \"(no tests yet — shared)\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
